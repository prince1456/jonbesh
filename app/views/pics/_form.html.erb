<% if current_user.present? %>
<div class=" report-form">
    <%= simple_form_for(pic) do |f| %>
    <% if pic.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(pic.errors.count, "error") %>
            prohibited this pic from being saved:</h2>

        <ul>
            <% pic.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>

    <%= f.input :title,:label => "لطفا در این بخش پیامتان را بنویسید", class:"form-control" %>

    <%= f.input :image, :label => "عکس"%>

    <%= f.input :Post_on_facebook, as: :boolean, checked_value: true, unchecked_value: false %>

    <%= f.submit  "آپلود عکس",class:"btn btn-default" %>

    <% end %>
</div>
<% else %>
<div class=" report-form">
    <%= simple_form_for(pic) do |f| %>
    <% if pic.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(pic.errors.count, "error") %>
            prohibited this pic from being saved:</h2>

        <ul>
            <% pic.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>

    <%= f.input :title,:label => "لطفا در این بخش پیامتان را بنویسید", class:"form-control" %>

    <%= f.input :image, :label => "عکس"%>



    <%= f.submit  "آپلود عکس",class:"btn btn-default" %>

    <% end %>
</div>
<% end %>
